{
  "name": "repocontext",
  "version": "0.1.1",
  "private": true,
  "description": "Personal local MCP server for safe, commit-pinned multi-repo context across the SHMindMaster portfolio.",
  "license": "MIT",
  "packageManager": "pnpm@11.15.0",
  "engines": { "node": ">=20" },
  "scripts": {
    "mcp:serve": "tsx src/server.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test": "vitest run",
    "validate": "tsc --noEmit && vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "simple-git": "^3.27.0",
    "yaml": "^2.7.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "tsx": "^4.19.0",
    "vitest": "^3.1.0",
    "@types/node": "^22.0.0"
  },
  "keywords": ["mcp", "ai-agents", "codebase", "multi-repo", "documentation", "context", "claude", "codex", "cursor", "model-context-protocol"]
}
