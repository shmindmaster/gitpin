schemaVersion: 1
manifestPath: .gitpin/change-evidence.json

coverage:
  include:
    - "**"
  exclude:
    - "docs/generated/**"
    - "package.json"

policyChanges: block

limits:
  changedPaths: 100
  claims: 50
  evidencePerClaim: 8
  fileBytes: 2097152
