name: GitPin Evidence Gate

on:
  pull_request:

permissions:
  contents: read

concurrency:
  group: evidence-gate-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  evidence:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha }}
      # Post-publication boundary: v0.6.2 is published and immutable; keep the self-gate pinned to that release.
      - uses: shmindmaster/gitpin@v0.6.2
        with:
          base-sha: ${{ github.event.pull_request.base.sha }}
          head-sha: ${{ github.event.pull_request.head.sha }}
