name: Copilot Setup Steps

on:
  workflow_dispatch:

jobs:
  copilot-setup-steps:
    runs-on: [self-hosted, Linux, X64, copilot, sh-runner-repocontext]
    timeout-minutes: 59

    steps:
      - name: Verify runner environment
        shell: bash
        run: |
          echo "Using self-hosted Copilot runner: $(hostname)"
          echo "Runner labels: copilot, sh-runner-repocontext"
