name: Copilot Setup Steps

on:
  workflow_dispatch:

jobs:
  copilot-setup-steps:
    runs-on: ubuntu-latest
    timeout-minutes: 59

    steps:
      - name: Verify runner environment
        shell: bash
        run: |
          echo "Using GitHub-hosted Copilot runner: $(hostname)"
          echo "Runner image: ${ImageOS:-unknown}"
