name: Copilot setup
description: Configure the Copilot agent execution environment for this repository.
runs:
  using: "composite"
  steps:
    - name: Mark runner context
      run: |
        echo "repo: gitpin"
        echo "runner-label: github-hosted"
