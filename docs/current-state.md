# Current State

Operational state of the `gitpin` repository as of the most recent verification.
Update this file whenever the project surface or its operational truth changes.

## Project

- **GitPin** is an index-free, read-only MCP server for multi-repo evidence pinned to
  Git HEAD: `pin.*` tools, `gitpin` CLI, and a PR evidence gate. No databases,
  embeddings, queues, or write tools.
- **Package:** `gitpin` (npm, MIT). **Homepage:** `https://shmindmaster.github.io/gitpin/`.
- **Published:** npm and GitHub Releases expose `0.6.3`; the MCP Registry and GitHub
  Pages still expose `0.6.2` pending explicit publication and independent verification.
  Historical releases remain immutable.

## Source layout

- `src/` — TypeScript sources (single package, one bounded responsibility per file).
- `dist/` — compiled output generated by `pnpm build` and included in the npm package;
  it is not committed to Git.
- `site/` — static project website sources; `.site-dist/` is the built output.
- `templates/` — client rules, wiki schema, gate policy shipped to consumers.
- `docs/` — durable documentation (see `docs/README.md`).

## CI/CD and automation

- GitHub Actions: `ci.yml` (lint/format/typecheck/verifiers/tests), `evidence-gate.yml`
  (PR evidence gate), `pages.yml` (site deploy), `publish-mcp.yml` (MCP registry),
  `release.yml` (npm release).
- npm publishes from tags on `main`; MCP publication is manually dispatched against a
  validated release tag. Pages is manually dispatched and requires independent deployed
  SHA and content verification.

## Local generated artifacts

`.repocontext-index/`, `.demo/`, `.site-dist/`, `playwright-report/`, and
`test-results/` are gitignored local artifacts. `.repowise/` holds the shared RepoWise
index state and is gitignored on purpose.

## Known issues / caveats

- The HTTP transport requires a bearer token configured at runtime; never commit it.
- `record-demo-qwen.ps1` recently migrated from the hardcoded `D:\AI-Platform` fallback
  to `LOCAL_AI_ROOT` (commit `105798c`); keep AI_PLATFORM_ROOT-style hardcoding out.

## Docs hygiene

- Standardized `README.md`, `AGENTS.md`, `CLAUDE.md`, and the `docs/` taxonomy
  (`docs/product`, `docs/architecture`, `docs/development`, `docs/runbooks`, `docs/plans`).
- `ROADMAP.md` retained at root as an OSS-standard project file (in the npm `files`
  allowlist), alongside `CHANGELOG.md`, `CONTRIBUTING.md`, `SECURITY.md`,
  `CODE_OF_CONDUCT.md`, and `LICENSE`.
