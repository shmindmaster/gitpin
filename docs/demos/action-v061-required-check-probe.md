# Released Action v0.6.1 required-check probe

This synthetic-only material change validates the production GitPin evidence check without modifying product behavior.

The validation sequence intentionally observes an uncovered-path failure first, then adds committed exact-line evidence and observes the same required check pass.
