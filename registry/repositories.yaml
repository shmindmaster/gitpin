# Public starter registry. Paths are resolved relative to this file, so this
# self-indexes after cloning and can be copied beside any repository collection.
# Every entry must point at a Git repository root.

repositories:
  - name: repocontext
    path: ..
    branches: [main]
